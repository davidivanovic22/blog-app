import { PaginationClearPipe } from './pagination-clear.pipe';

describe('PaginationClearPipe', () => {
  it('create an instance', () => {
    const pipe = new PaginationClearPipe();
    expect(pipe).toBeTruthy();
  });
});
